<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uh.elefit">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/elefit_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/elefit_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Splash screen -->
        <activity
            android:name=".SplashScreen"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main activity -->

        <activity
            android:name=".Liftovi"
            android:label="@string/title_activity_liftovi"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Main2"
            android:label="@string/title_activity_main2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Grafikon"
            android:label="@string/title_activity_grafikon"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MojiServisi"
            android:label="@string/title_activity_moji_servisi"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DetaljiLifta"
            android:label="@string/title_activity_detalji_lifta"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Login"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".Prijava"
            android:label="@string/title_activity_prijava"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>